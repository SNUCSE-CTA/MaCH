# Subhypergraph-Matching

compile
```bash
mkdir build
cd build
cmake ..
```

```bash
./SubHypergraphMatching -d [path_to_indexed_file] -q [path_to_query]
```

Obtain ./[dataset].out index file by: 
```bash
./HypergraphPreprocessing -p [path_to_datasets] -d [dataset_name]
```
